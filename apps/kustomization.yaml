apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
    - hello-nginx/hello-nginx-app.yaml
    #- openeo-argoworkflows/overlays/stable enable once ready
    - openeo-argoworkflows/overlays/dev
    - monitoring/
    - dask-gateway/
